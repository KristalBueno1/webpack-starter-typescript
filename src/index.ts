
import db "./firebase/config";

const usuario = {
    nombre: "Fernando",
    activo: true,
    fechaNaci: 0
}

// insert into usuarios...

// usuarios_ref
    //     .add(usuario)

    //         /db.collection('usuarios')
    // .add(usuario)
    // .then( docRef => {
    //    console.log( docRef )
    // })
    // .catch(e => console.log('error', e))